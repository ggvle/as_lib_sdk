<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.yline.view.layout.demo">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"></action>
                <category android:name="android.intent.category.LAUNCHER"></category>
            </intent-filter>
        </activity>
        <activity android:name=".ad.WidgetADActivity"/>
        <activity android:name=".label.WidgetFlowAble3Activity"/>
        <activity android:name=".label.WidgetFlowAbleActivity"/>
        <activity android:name=".label.WidgetFlowActivity"/>
        <activity android:name=".label.WidgetFlowSingleActivity"/>
        <activity android:name=".InnerShowActivity"/>
    </application>

</manifest>
